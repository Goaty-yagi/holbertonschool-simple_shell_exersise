#include "main.h"

int main(int ac, char **av)
{
    char **ap;

    (void)ac;
    (void)ap;
    (void)av;
    // file_info_ex(ac, av);
    file_info_exercise(ac, av);
    // opendir_readdir();
    // printf("%s", ap[8]);
    // ap = get_dir(".");
    return (0);
}
